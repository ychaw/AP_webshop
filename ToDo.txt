Needs implementation:
  - password hash
  - session variables to store login-status, admin-rights, items in basket
  - file upload
  - item display

Maybe needs implementation:
  - mail-address for users

Needs fixing:
  - Wrong password when deleting account causes crash. Probably async issue
